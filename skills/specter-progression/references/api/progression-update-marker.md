# Client API (v2): `progression/update-marker`

**Endpoint:** `POST /v2/client/progression/update-marker`

**Tag:** Progression

**Summary:** Update progression marker

**Auth:** Project **api-key** (`api-key` header) **+ player access token** (`Authorization: Bearer <playerAccessToken>`). Player-scoped — call after logging the player in.

---

## Request body — `UpdateProgressionWalletDto`

_No documented fields._

