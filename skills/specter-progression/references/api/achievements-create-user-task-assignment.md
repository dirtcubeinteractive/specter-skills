# Client API (v2): `achievements/create-user-task-assignment`

**Endpoint:** `POST /v2/client/achievements/create-user-task-assignment`

**Tag:** Achievements

**Summary:** Create user task assignment record

**Auth:** Project **api-key** (`api-key` header) **+ player access token** (`Authorization: Bearer <playerAccessToken>`). Player-scoped — call after logging the player in.

---

## Request body

_No request body (send `{}`)._
