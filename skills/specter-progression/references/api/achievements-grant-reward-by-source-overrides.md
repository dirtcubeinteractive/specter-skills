# Client API (v2): `achievements/grant-reward-by-source-overrides`

**Endpoint:** `POST /v2/client/achievements/grant-reward-by-source-overrides`

**Tag:** Achievements

**Summary:** Grant rewards by source with overrides

**Auth:** Project **api-key** (`api-key` header) **+ player access token** (`Authorization: Bearer <playerAccessToken>`). Player-scoped — call after logging the player in.

---

## Request body — `GrantRewardBySourceOverridesV2Dto`

_No documented fields._

