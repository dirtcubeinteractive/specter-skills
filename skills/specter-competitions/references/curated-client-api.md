# specter-competitions — curated client (v2) API reference

> Generated by `scripts/generate-curated-references.mjs` from Specter's own API-assistant
> config. Human-written descriptions + parameter requiredness, types, and `enumValues`
> (especially the `attributes` you can request on v2 client reads). All `POST /v2/client/*`.

## Competitions

### Get Competition Entries

`POST /v2/client/competition/get-entries`

Fetches entries for specific competitions, with options to filter by competition IDs, format types, match IDs, game IDs, and attributes.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionIds` | array<string> | — |  | Array of competition IDs to filter entries. |
| `formatTypes` | array<string> | — |  | Array of format types to filter entries. |
| `matchIds` | array<string> | — |  | Array of match IDs to filter entries. |
| `gameIds` | array<string> | — |  | Array of game IDs to filter entries. |
| `offset` | number | — |  | Pagination offset for results. |
| `limit` | number | — |  | Pagination limit for the number of results to retrieve. |
| `instanceId` | string | — |  | InstanceId of competition to filter entries. |


### Check Attempts

`POST /v2/client/competitions/check-attempts`

Checks the attempts made by the user with the specified entry ID.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `entryId` | string | ✅ |  | The unique identifier of the entry to check attempts for. |


### Enter Competition

`POST /v2/client/competitions/enter`

Allows a user to enter a competition with a unique competition ID, optional instance ID, entry ID, and custom parameters.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionId` | string | ✅ |  | The unique identifier for the competition. |
| `customParams` | object | — |  | Additional custom parameters for the competition entry. |


### Enter RMG Competition

`POST /v2/client/competitions/enter-rmg-competition`

Enters a player into a RMG competition using the provided competition identifier, and optional instance, entry, and custom parameters.

**Required headers:** `Api-Key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionId` | string | ✅ |  | The unique identifier of the competition. This field is required. |
| `entryId` | string | — |  | Optional identifier for the competition entry. |
| `customParams` | object | — |  | Optional object containing additional custom parameters for the competition entry. |


### Get Prize Pool

`POST /v2/client/competitions/get-current-prize-pool`

Retrieves the current prize pool for a RMG competition using the provided competition and instance identifiers.

**Required headers:** `Api-Key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionId` | string | ✅ |  | Unique identifier for the competition. |
| `instanceId` | string | ✅ |  | Identifier for the specific instance of the competition. |


### Get Instant Battle Result

`POST /v2/client/competitions/get-instantbattle-results`

Retrieves the results of an Instant Battle for a specified competition and entry ID, with optional attributes and pagination options.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionId` | string | ✅ |  | The unique identifier for the instant battle competition. |
| `entryId` | string | ✅ |  | The unique identifier of the entry in the instant battle. |
| `offset` | number | — |  | Pagination offset for the results. |
| `limit` | number | — |  | Pagination limit for the number of results to retrieve. |
| `attributes` | array<enum> | — | `rankings` \| `totalRanks` | Array of attributes to include in the response |


### Get Tournament Ranking

`POST /v2/client/competitions/get-tournament-rankings`

Fetches the ranking data for a tournament, filtered by competition ID and optional attributes, with pagination options.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionId` | string | ✅ |  | The unique identifier for the tournament competition. |
| `instanceId` | string | ✅ |  | The unique instance identifier for the tournament. |
| `offset` | number | — |  | Pagination offset for the results. |
| `limit` | number | — |  | Pagination limit for the number of results to retrieve. |
| `attributes` | array<enum> | — | `rankings` \| `totalRanks` | Array of attributes to include in the response |


### Get Tournament Result

`POST /v2/client/competitions/get-tournament-results`

Retrieves the results of a tournament, filtered by competition ID and optional attributes, with pagination options.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionId` | string | ✅ |  | The unique identifier for the tournament competition. |
| `instanceId` | string | ✅ |  | The unique instance identifier for the tournament. |
| `offset` | number | — |  | Pagination offset for results. |
| `limit` | number | — |  | Pagination limit for the number of results to retrieve. |
| `attributes` | array<enum> | — | `rankings` \| `totalRanks` | Array of attributes to include in the response |


### Post Score to Tournament

`POST /v2/client/competitions/post-score-to-tournament`

Submits a score to a tournament for a specified entry and competition ID.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionId` | string | — |  | The unique identifier of the competition (optional). |
| `entryId` | string | ✅ |  | The unique identifier of the entry to which the score belongs. |
| `score` | number | ✅ |  | The score to submit for the entry. |


## Leaderboards

### Get Leaderboard Rankings

`POST /v2/client/leaderboards/get-rankings`

Retrieves ranking data for a specific leaderboard, filtered by attributes, with pagination options.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `leaderboardId` | string | ✅ |  | The unique identifier for the leaderboard to retrieve rankings from. |
| `instanceId` | string | ✅ |  | The unique instance identifier for the leaderboard session. |
| `limit` | number | — |  | Pagination limit for the number of rankings to retrieve. |
| `offset` | number | — |  | Pagination offset for rankings. |
| `attributes` | array<enum> | — | `rankings` \| `totalRanks` | Array of attributes to include in the response |


### Get Leaderboard Results

`POST /v2/client/leaderboards/get-results`

Fetches results from a specific leaderboard, with options to filter by attributes and paginate results.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `leaderboardId` | string | ✅ |  | The unique identifier for the leaderboard to retrieve results from. |
| `instanceId` | string | ✅ |  | The unique instance identifier for the leaderboard session. |
| `limit` | number | — |  | Pagination limit for the number of results to retrieve. |
| `offset` | number | — |  | Pagination offset for results. |
| `attributes` | array<enum> | — | `rankings` \| `totalRanks` | Array of attributes to include in the response |


### Post Score to Leaderboard

`POST /v2/client/leaderboards/post-score`

Submits a score to multiple leaderboards for a specified competition slot and leaderboard IDs.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `leaderboardIds` | array<string> | ✅ |  | Array of leaderboard IDs where the score will be posted. |
| `score` | number | ✅ |  | The score to submit to the leaderboard(s). |


## Live Ops

### Get Competition Schedule 

`POST /v2/client/liveops/get-competition-schedule-history`

Retrieves the schedule history for specified competitions, with optional pagination.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `competitionIds` | array<string> | — |  | Array of competition IDs for which to retrieve schedule history. |
| `limit` | number | — |  | Pagination limit for the number of results to retrieve. |
| `offset` | number | — |  | Pagination offset for the results. |


### Get Leaderboard Schedule

`POST /v2/client/liveops/get-leaderboard-schedule-history`

Fetches the schedule history for specified leaderboards, with optional pagination.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `leaderboardIds` | array<string> | — |  | Array of leaderboard IDs for which to retrieve schedule history. |
| `limit` | number | — |  | Pagination limit for the number of results to retrieve. |
| `offset` | number | — |  | Pagination offset for the results. |


### Get Server Time

`POST /v2/client/liveops/get-server-time`

Retrieves the current server time, optionally adjusted to a specified timezone.

**Required headers:** `api-key`, `Authorization`

| Param | Type | Required | Allowed values | Description |
|---|---|---|---|---|
| `timezone` | string | — |  | Optional timezone for formatting the server time response. |

