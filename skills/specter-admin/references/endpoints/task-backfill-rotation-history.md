# Admin API: `task/backfill-rotation-history`

**Endpoint:** `POST /v1/task/backfill-rotation-history`

**Tag:** Task

**Summary:** Backfill rotation history

**Auth:** Member Bearer token (`Authorization: Bearer <token>`) through the api-key gateway (`api-key` header). Get the token via the MCP browser sign-in — never ask the user for a password.

---

## Request body

_No request body (or not documented in the spec)._
