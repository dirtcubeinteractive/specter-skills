# Admin API: `organisation/get-subscription-status`

**Endpoint:** `POST /v1/organisation/get-subscription-status`

**Tag:** Organisation

**Summary:** POST /v1/organisation/get-subscription-status

**Auth:** Member Bearer token (`Authorization: Bearer <token>`) through the api-key gateway (`api-key` header). Get the token via the MCP browser sign-in — never ask the user for a password.

---

## Request body — `GetSubscriptionStatusDto`

_No documented fields._

