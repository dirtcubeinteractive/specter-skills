# Admin API: `leaderboard/get-leaderboard-details`

**Endpoint:** `POST /v1/leaderboard/get-leaderboard-details`

**Tag:** Leaderboards

**Summary:** POST /v1/leaderboard/get-leaderboard-details

**Auth:** Member Bearer token (`Authorization: Bearer <token>`) through the api-key gateway (`api-key` header). Get the token via the MCP browser sign-in — never ask the user for a password.

---

## Request body — `getLeaderboardDetailsDto`

_No documented fields._

